

import {
  {{endpoint.interfaceName}}RequestParams,
  {{endpoint.interfaceName}}RequestBody,
  {{endpoint.interfaceName}}ValidationResult
} from './{{routePath}}';
import { StatusCodes } from 'http-status-codes';

{{#if endpoint.handler.validation.class}}export class {{endpoint.handler.validation.class}} {
    public {{endpoint.handler.validation.function}}(
        param: {{endpoint.interfaceName}}RequestParams, 
        body: {{endpoint.interfaceName}}RequestBody
    ): {{endpoint.interfaceName}}ValidationResult {
        return {
            statusCode: StatusCodes.OK
        }
    };
}
{{else}}
export function {{endpoint.handler.validation.function}}(
    param: {{endpoint.interfaceName}}RequestParams, 
    body: {{endpoint.interfaceName}}RequestBody
): {{endpoint.interfaceName}}ValidationResult {
    return {
        statusCode: StatusCodes.OK
    };
}
}{{/if}}
