{{#addFunctionAndInterfaceNames name endpoint}}

import {
  {{interfaceName}}RequestParams,
  {{interfaceName}}RequestBody,
  {{interfaceName}}HandlerResponse,
} from "./backend_test_route";

{{#if handler.request.class}}export class {{handler.request.class}} {
    public {{function}}(
        param: {{interfaceName}}RequestParams, 
        body: {{interfaceName}}RequestBody
    ): {{interfaceName}}HandlerResponse {
        return undefined;
    }
}
{{else}}
export function {{handler.request.function}}(
    param: {{interfaceName}}RequestParams, 
    body: {{interfaceName}}RequestBody
): {{interfaceName}}HandlerResponse {
    return undefined;
}
}{{/if}}

{{/addFunctionAndInterfaceNames}}